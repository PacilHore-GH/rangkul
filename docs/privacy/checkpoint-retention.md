# Checkpoint Retention

The default retention mode is `privacy_first`. Raw media should expire after `CHECKPOINT_RAW_RETENTION_DAYS` while structured derivatives needed for approved reports can remain according to policy. All raw-media access, retention deletion, reprocessing, and professional approvals require audit events.
