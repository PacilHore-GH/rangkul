# Model Bootstrap Runbook

Run `make models-download` only in an environment allowed to download public weights. Run `make models-verify` before worker startup. Production should mount the verified cache read-only.
